from pydantic import BaseModel
from typing import Optional

class ModelConfig(BaseModel):
    """Конфигурация для модели glm-4.5-flash"""
    
    # Основные параметры модели
    model_name: str = "glm-4.5-flash"
    max_tokens: int = 4096
    temperature: float = 0.95
    
    # Параметры для reasoning (не поддерживается в стандартном API)
    # thinking_enabled: bool = True
    
    # Системный промпт для агента
    system_prompt: str = """Ты - помощник по инструментам сайта realtycalendar.ru. 
Твоя задача - отвечать на вопросы пользователей о доступных инструментах на основе предоставленных данных.

Правила:
1. Отвечай только на русском языке
2. Используй информацию только из предоставленных данных об инструментах
3. Если вопрос не связан с инструментами, вежливо перенаправь к теме
4. Будь дружелюбным и полезным
5. Если не знаешь ответа, честно скажи об этом
6. Предлагай дополнительные вопросы, которые могут быть полезны пользователю"""

    # Параметры для API
    api_timeout: int = 30
    max_retries: int = 3
    
    class Config:
        env_prefix = "MODEL_"
